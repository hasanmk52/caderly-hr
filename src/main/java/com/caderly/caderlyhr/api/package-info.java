/**
 * REST controllers under {@code /api/v1} (CLAUDE.md §4). Every prior phase built its UI as a
 * server-rendered {@code web} htmx controller instead, so this package is empty until sub-phase
 * 1.8's iCal feed — the first surface in the app consumed by an external, non-browser client
 * rather than the app's own Thymeleaf pages. See {@code calendar.CalendarFeedService}'s Javadoc.
 */
@NullMarked
package com.caderly.caderlyhr.api;

import org.jspecify.annotations.NullMarked;
